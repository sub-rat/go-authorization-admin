import MainLayout from "../../components/layouts/wrappers/main-layout"

const Layout = ({ children }: { children: React.ReactNode }) => (
  <MainLayout>{children}</MainLayout>
)

export default Layout
